import Page from '@/components/Layout/Page'
import { WEBSITE_NAME } from '@/lib/constants'

const Products = () => (
  <Page title={`Products | ${WEBSITE_NAME}`}>
    <h1>Products Page</h1>
  </Page>
)

export default Products
