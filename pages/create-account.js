import Page from '@/components/Layout/Page'
import { WEBSITE_NAME } from '@/lib/constants'

const CreateAccount = () => (
  <Page title={`Run your business with more confidence | ${WEBSITE_NAME}`}>
    <h1>Create Account Page</h1>
  </Page>
)

export default CreateAccount
